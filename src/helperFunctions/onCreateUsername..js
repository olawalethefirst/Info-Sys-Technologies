// import updateUsernameAsync from './updateUserDatabaseAsync';

const onUpdateUsername = (username) => {
    // upon succesful or failed update, navigate to home screen for handling by username modal if failed and normal usage if passed
    // updateUsernameAsync(username).then(
    //     () => console.log('successful'),
    //     () => console.log('failed')
    // );
};

export default onUpdateUsername;
